
<h1>Here are your tasks:</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= task[:name] %> (<%= task[:status] %>) <%= link_to "See Details", task_path(task)  %> </li>
  <% end %>
</ul>

<%= link_to "Add a task", new_task_path %>
